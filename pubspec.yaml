name: custom_embed_quill
description: A new Flutter project.
$publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.0-190.0.dev <3.0.0'


dependencies:
  flutter:
    sdk: flutter
  visual_editor:
    git: https://github.com/visual-space/visual-editor.git

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
